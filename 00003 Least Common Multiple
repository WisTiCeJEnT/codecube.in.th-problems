#include <iostream>
//#include <math.h>
//#include <stdlib.h>
//#include <iomanip>
//#include <vector>

using namespace std;

int main()
{
	int a,b,A,B;
	cin >> a >> b;
	A = a;
	B = b;
	while(A!=B)
	{
		if(B<A)
			B = B + b;
		else
			A = A + a;
	}
	cout << A;
	return 0;
}